# Today-I-Learned

##### Date: 2025-11-14

# 1️⃣ 오늘 배운 것
- '에라토스테네스의 체'에 관한 불확실했던 개념 정리.
- 자연수가 주어졌을 때, 얻을 수 있는 생성자 중 가장 작은 수 구하는 방법
- '모듈러 공식'에 관한 불확실했던 개념 정리.
- '유클리드 호제법' 알고리즘 개념 확립.
- '카운팅 정렬' 사용 개념 확립.
- - -
# 2️⃣ 참고 문제 / 자료
1. 백준_1978번: 소수 찾기
2. 백준_2231번: 분해합
3. 백준_15829번: Hashing
4. 백준_2609번: 최대공약수와 최소공배수
5. 백준_10989번: 수 정렬하기 3
- - -
# 3️⃣ 정리
  ## 1. 백준_1978번: 소수 찾기
  ### 🖥 사용 개념
  - '에라토스테네스의 체'
- - -
  ### 🛠 개념 정리
  **1. '에라토스테네스의 체'**
  * 수열에서 소수를 구하는 알고리즘.
  * 수의 범위가 기재된 경우 0부터 해당 범위까지의 수들 중에서 boolean[]을 통해 소수인지 아닌지 전처리에서 구별함.
  * 만약 수의 범위(N)가 1000인 경우, boolean 배열의 isPrime을 N+1 크기만큼 생성해서 모든 값을 true로 초기화 후, 0과 1 제외, 짝수 제외, 이후 나오는 수 중 가장 작은 수는 소수로 가정하고 해당 수의 배수는 모두 제외하는 방식.
  * 개수를 얻을 경우엔 조건문을 통해 true인 경우만 count, 최소값을 얻고 싶다면 조건문을 통해 최초로 얻는 값을 출력.
  * 만약 난수로 표시된 경우, 해당 값을 조건문으로 넣어 isPrime에서 해당 값이 true인 경우에만 원하는 출력값을 얻을 수 있도록 함.
<pre>
<code>
  public static boolean[] sieve(int n){
        // return값 생성. run() 메소드에서 isPrime 배열을 선언했기 때문.
        boolean[] isPrime = new boolean[n + 1];

        // isPrime 배열 모든 요소값을 true로 설정.
        for (int i = 0; i < n + 1; i++) isPrime[i] = true;

        // n이 1개만 존재할 경우, 2개만 존재할 경우를 생각해서 if문 포함. 0과 1 제외.
        if (n >= 0) isPrime[0] = false;
        if (n >= 1) isPrime[1] = false;

        // 루트 n값까지 돌려서 isPrime 배열 속 i번째 값이 true면 조건문 진입. 2부터 시작함.
        int limit = (int) Math.sqrt(n);
        for (int i = 2; i < limit; i++){
            // 2의 경우 true이기 떄문에 진입, 2의 배수 모두 제외.
            // 이후 조건문에 진입하는 값이 true라면 그 수의 모든 배수는 제외됨.
            if (isPrime[i]){
                for (int j = i * i; j <= n; j += i){
                    isPrime[j] = false;
                }
            }
        }
        return isPrime;
  }
</code>
</pre>
- - -
  ### 🔍 해결 방법
  1. '에라토스테네스의 체'을 이용해서 0부터 입력될 수 있는 수의 범위까지 얻을 수 있는 모든 자연수의 소수 여부를 판별할 수 있는 함수를 전처리로 처리한 후, 원하는 출력값을 얻을 수 있도록 if(isPrime[index])를 활용해서 얻는 방법.
  2. '개별 판별 방식'을 활용해서 일일이 소수 구별 방식을 활용하는 방법. (0과 1일 경우 제외, 짝수일 경우 제외, √(최대 범위) 까지 반복해서 해당 수와 나눴을 때 나눠지는 수가 있는 경우 제외.)
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
  import java.util.*;
  import java.io.*;
  
  public class a_1978{
      BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
      StringTokenizer st;
      public static boolean[] sieve(int n){
          // return값 생성. run() 메소드에서 isPrime 배열을 선언했기 때문.
          boolean[] isPrime = new boolean[n + 1];
  
          // isPrime 배열 모든 요소값을 true로 설정.
          for (int i = 0; i < n + 1; i++) isPrime[i] = true;
  
          // n이 1개만 존재할 경우, 2개만 존재할 경우를 생각해서 if문 포함. 0과 1 제외.
          if (n >= 0) isPrime[0] = false;
          if (n >= 1) isPrime[1] = false;
  
          // 루트 n값까지 돌려서 isPrime 배열 속 i번째 값이 true면 조건문 진입. 2부터 시작함.
          // 수정_2025-11-18 || i < limit 부분을 i <= limit으로 수정.
          int limit = (int) Math.sqrt(n);
          for (int i = 2; i <= limit; i++){
              // 2의 경우 true이기 떄문에 진입, 2의 배수 모두 제외.
              // 이후 조건문에 진입하는 값이 true라면 그 수의 모든 배수는 제외됨.
              if (isPrime[i]){
                  for (int j = i * i; j <= n; j += i){
                      isPrime[j] = false;
                  }
              }
          }
          return isPrime;
      }
  
      public void run() throws IOException{
          int n = Integer.parseInt(br.readLine());
          st = new StringTokenizer(br.readLine());
          
          boolean[] isPrime = sieve(1000);
  
          int count = 0;
          for (int i = 0; i < n; i++){
              int num = Integer.parseInt(st.nextToken());
              if (isPrime[num]){
                  count++;
              }
          }
          System.out.println(count);
          br.close();
      }
      public static void main(String[] args) throws IOException{
          try{
              new a_1978().run();
          } catch (IOException e){
              System.err.println(e.getMessage());
          }
      }
  }
  </code>
  </pre>
- - -
  ## 2. 백준_2213번: 분해합
  ### 🖥 사용 개념
  - '분해합과 생성자'
- - -
  ### 🛠 개념 정리
  **1. '분해합과 생성자'**
  * 어떤 자연수 M의 분해합 T = (M + 자연수(M) 각 자리의 합).
  * T의 값이 어떤 자연수(N)과 같을 때, M = N의 생성자
  * 만약 어떤 자연수 N을 얻었을 때, 생성자의 범위는 다음과 같음.
      * N = M + (각 자리의 합) <= M + (9(각 자리마다 가질 수 있는 최대의 수) * d(N의 자리수)) => 최대로 얻을 수 있는 생성자
      * N - (9 * d) <= M => 최소로 얻을 수 있는 생성자
      * 어떤 자연수의 생성자이기 때문에 0과 음수는 가정하지 않음. 이런 경우 1로 전환.
  * 반복문을 이용해 최소 생성자와 최대 생성자를 구할 수 있음.
      * 최소 생성자
          * start = Math.max(1, N - (9 * d))
          * end = N - 1
          * i++
      * 최대 생성자
          * start = N - 1
          * end = Math.max(1, N - (9 * d))
          * i--
<pre>
<code>
      int start = Math.max(1, Integer.parseInt(N) - (9 * d)); //216 - 27 = 189
      if (start <= 0) start = 1;
      int sum = 0;
      int result = 0;
      for (int i = start; i < Integer.parseInt(N); i++){ //189 ~ 215
          sum = i;
          int value = i;
          while (value > 0){
              sum += value % 10;
              value /= 10;
          }
          if (sum == Integer.parseInt(N)) {
              result = i;
              break;
          } else if (sum != Integer.parseInt(N) && i == Integer.parseInt(N) - 1){
              result = 0;
              break;
          }
      }
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 해당 문제는 최소 생성자를 찾는 문제였기 때문에 개념에 기재한 코드를 활용해서 문제를 해결함. 최대의 경우 반복문의 조건을 반대로 해서 큰 수부터 내려오는 방식으로 찾으면 됨.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    public class a_2231{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        public void run() throws IOException{
            String N = br.readLine();
            int d = N.length();
            int start = Math.max(1, Integer.parseInt(N) - (9 * d)); //216 - 27 = 189
            if (start <= 0) start = 1;
            int sum = 0;
            int result = 0;
            for (int i = start; i < Integer.parseInt(N); i++){ //189 ~ 215
                sum = i;
                int value = i;
                while (value > 0){
                    sum += value % 10;
                    value /= 10;
                }
                if (sum == Integer.parseInt(N)) {
                    result = i;
                    break;
                } else if (sum != Integer.parseInt(N) && i == Integer.parseInt(N) - 1){
                    result = 0;
                    break;
                }
            }
            System.out.println(result);
            br.close();
        }
              
        public static void main(String[] args) throws IOException{
            try{
                new a_2231().run();
            } catch(IOException e){
                System.err.println(e.getMessage());
            }
        }
    }
  </code>
  </pre>
- - -
  ## 3. 백준_15829번: Hashing
  ### 🖥 사용 개념
  - '모듈러 공식'
- - -
  ### 🛠 개념 정리
  **1. '모듈러 공식'**
  * mod (자연수)로 사용됨. 일반적으로 %(나머지)의 의미를 담고 있음.
  * 나머지를 구하는 공식이기 때문에 식에 여러 번 기입해도 결국 해당 자연수보다 큰 수가 되지 않으면 계산에 영향이 없음.
  * 그렇기 때문에 수의 크기가 감당이 안 될 경우, 식의 결과에 영향이 안 간다면 mod를 먼저 계산하는 경우도 있음.
  * 예시는 다음과 같음.
    * (17 + 23) mod 5
        1. 40 % 5 = 0
        2. ((17 mod 5) + (23 mod 5)) mod 5 => (2 + 3) mod 5 => 5 % 5 = 0
    - 동일한 출력값을 가지는 것을 확인할 수 있음.
  * 정의를 정리하면 다음과 같음.
    - (a + b) mod M = ((a mod M) + (b mod M)) mod M
    - (a * b) mod M = ((a mod M) * (b mod M)) mod M
<pre>
<code>
      long M = 1234567891;
      long sum = 0;
      long pow = 1;
      for (int i = 0; i < L; i++){
          int num = input.charAt(i) - 'a' + 1;
          sum = (sum + num * pow) % M;
          pow = (pow * 31) % M;
      }
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 개념 정리에 적었던 내용을 반영해서 코드를 작성함. 이 코드에서 pow이 조금 이해안 될 수 있는데, mod M을 여러 번 기입해도 결과값은 동일하게 적용됨. 그렇기 때문에 미리 적용하여 수의 크기를 줄이는 것임을 확인할 수 있다.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
  
    public class a_15829 {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        public void run() throws IOException{
            int L = Integer.parseInt(br.readLine());
            String input = br.readLine();
            long M = 1234567891;
            long sum = 0;
            long pow = 1;
            for (int i = 0; i < L; i++){
                int num = input.charAt(i) - 'a' + 1;
                sum = (sum + num * pow) % M;
                pow = (pow * 31) % M;
            }
            System.out.println(sum);
            br.close();
        }   
              
        public static void main(String[] args) throws IOException{
            try{
                new a_15829().run();
            } catch (Exception e){
                System.err.println(e.getMessage());
            }
        }
    }
  </code>
  </pre>
- - -
  ## 4. 백준_2609번: 최대공약수와 최소공배수
  ### 🖥 사용 개념
  - '유클리드 호제법'
- - -
  ### 🛠 개념 정리
  **1. '유클리드 호제법'**
  * 최대공약수와 최소공배수를 구할 때 사용하는 알고리즘.
  * 최대공약수를 구하는 방법은 다음과 같음.
    * a는 a를 b로 나눈 값 (몫 * b) + 나머지(r)로 구성됨. 나머지가 0이 아닐 경우 새로운 a = b, 새로운 b = r로 해서 반복 계산함. 나머지가 0이 될 경우, 해당 식 이전의 나머지가 최대공약수.
        1. a = a / b + (a % b)(= r)
        2. b = b / r + (b % r)(= r2)
        3. r = r / r2 + (r % r2)(= r3)
        4. ...
    * a = 24, b = 18로 예시를 들면 다음과 같음.
        1. 24 = 24 / 18 + 24 % 18(=6)
        2. 18 = 18 / 6 + 18 % 6(=0)
      - 최대공약수는 6.

  * 최소공배수를 구하는 방법은 다음과 같음.
      * 최초로 주어진 자연수(a, b)의 곱을 최대공약수로 나눈 몫이 최소공배수.
          - (a * b) / (두 수의 최대공약수)
      * a = 24, b = 18로 예시를 들면 다음과 같음.
          - (24 * 18) / 6 = 432 / 6 = 72
        - 최소공배수는 72.
<pre>
<code>
        int r = 0;
        int i1 = a;
        int i2 = b;
        while (true){
            r = i1 % i2;
            if (r == 0) break;
            i1 = i2;
            i2 = r;
        }
        int max_sub = i2; // 최대공약수
        int min_mul = (a * b) / max_sub; // 최소공배수
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 유클리드 호제법을 이용해서 받은 입력값을 활용해서 최대공약수를 먼저 얻고, 이를 이용해서 최소공배수를 얻어 출력하는 코드를 작성하면 된다.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    import java.util.*;
    
    public class a_2609 {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
        StringBuilder sb = new StringBuilder();
        public void run() throws IOException{
            st = new StringTokenizer(br.readLine());
            int a = Integer.parseInt(st.nextToken());
            int b = Integer.parseInt(st.nextToken());
            int r = 0;
            int i1 = a;
            int i2 = b;
            while (true){
                r = i1 % i2;
                if (r == 0) break;
                i1 = i2;
                i2 = r;
            }
            int max_sub = i2;
            sb.append(max_sub).append("\n");
            int min_mul = (a * b) / max_sub;
            sb.append(min_mul);
            System.out.println(sb);
            br.close();
        }   
        public static void main(String[] args) throws IOException{
            try{
                new a_2609().run();
            } catch (Exception e){
                System.err.println(e.getMessage());
            }
        } 
    }
  </code>
  </pre>
- - -
  ## 4. 백준_10989번: 수 정렬하기 3
  ### 🖥 사용 개념
  - '카운팅 정렬'
- - -
  ### 🛠 개념 정리
  **1. '카운팅 정렬'**
  * 시간 제한과 메모리 제한이 걸린 상황에서, 퀵 정렬로도 시간 제한이 걸릴 경우 사용 가능한 정렬 방법.
  * 입력값의 범위가 표시되어있고, 난수로 이루어진 여러 개의 입력값을 입력 받을 경우에 사용하기 좋은 정렬.
  * 입력값을 하나씩 받아서 해당 입력값과 동일한 인덱스를 가진 배열 value를 +1 하고, 이후 다시 반복문을 이용해서 해당 value만큼 출력하는 방식.
  * 오름차순, 내림차순 출력 조건이 제시된다면 해당 조건에 맞춰 반복문 조건을 변경하면 됨. 오름차순의 경우 0 ~ MAX값까지, 내림차순의 경우 MAX ~ 0까지 범위 지정. 
<pre>
<code>
    final int MAX = 10_000; // 입력 가능한 최대값
        
    int n = Integer.parseInt(br.readLine());
    int[] arr = new int[MAX + 1]; // 배열 생성
    for (int i = 0; i < n; i++) {
      int input = Integer.parseInt(br.readLine()); // 입력값을 하나씩 받아서
      arr[input]++;  // 해당 입력값과 동일한 인덱스의 value 값 + 1
    }
    for (int i = 1; i <= MAX; i++) {
      if (arr[i] != 0) {  //value가 0 이상일 경우에만 진입
          while (arr[i] --> 0) sb.append(i).append("\n"); // value가 0이 될 때까지 해당 index 값을 반복해서 StringBuilder에 쌓음.  
      }
    }
    sb.substring(sb.length()); // StringBuilder 마지막에 사용된 newline 제거.
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 퀵 정렬을 시도해봤지만, 시간 초과로 여러 번 실패하게 됨. 이후 알게된 카운팅 정렬을 이용하여 sort 방식을 사용하지 않고 갯수를 세서 그만큼 반복하는 정렬을 이용. 시간과 메모리 조건을 충족하게 되었음.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    
    public class a_10989{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        final int MAX = 10_000;
        public void run() throws IOException{
            int n = Integer.parseInt(br.readLine());
            int[] arr = new int[MAX + 1];
            for (int i = 0; i < n; i++) {
                int input = Integer.parseInt(br.readLine());
                arr[input]++;
            }
            for (int i = 1; i <= MAX; i++) {
                if (arr[i] != 0) {
                    while (arr[i] --> 0) sb.append(i).append("\n");
                }
            }
            sb.substring(sb.length());
            System.out.print(sb);
            br.close();
        }
        public static void main(String[] args) throws IOException{
            try{
                new a_10989().run();
            } catch (IOException e){
                System.err.println(e.getMessage());
            }
        }
    }
  </code>
  </pre>
- - -
