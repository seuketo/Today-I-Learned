# Today-I-Learned

##### Date: 2025-05-26

## 1️⃣ 오늘 배운 것
오늘은 백준을 통해 알고리즘 문제를 해결하였다.
- - -
## 2️⃣ 참고 문제, 혹은 자료
1. 백준_1308번: D-Day
- - -
## 3️⃣ 정리
  ### 1. 백준_1308번: D-Day
  #### 🖥 사용 개념
  - JAVA - time 패키지 (LocalDate, ChronoUnit)
- - -
  #### 🛠 개념 정리
  **1. time 패키지 (java.time.*)**
  * 날짜와 시간, 요일을 다루는 패키지.
  * 윤년을 자동으로 적용하며, 연산 가능함.
<pre>
<code>
  // LocalDate (java.time.LocalDate)
  LocalDate d1 = LocalDate.of(2022, 2, 12)
             = LocalDate.of(Integer.parseInt(st.nextToken), Integer.parseInt(st.nextToken), Integer.parseInt(st.nextToken))

  // ChronoUnit (java.time.temporal.ChronoUnit)
  long days = ChronoUnit.DAYS.between(d1, d2)
  long months = ChronoUnit.MONTHS.between(d1, d2)
  long years = ChronoUnit.YEARS.between(d1, d2)
</code>
</pre>
- - -
  #### 📃 문제 정리
  - 윤년을 고려한 문제로, 1000년 이상 차이나는 경우, 혹은 1000년 차이나고 월, 일 단위의 수가 같은 경우 "gg"를 출력.
  - 그 외의 경우엔 D-(남은 일수) 출력해야함.
  - time 패키지를 사용하지 않을 경우엔 윤년을 고려할 수 있는 조건을 일일이 작성하고, 일, 월, 년 순서대로 계산해야 함.
    달마다 가지는 일수가 다 다르고, 윤년인 경우엔 2월의 총 일수 + 1일이기 때문에 고려사항이 많아짐.
  #### 🔍 해결 방법
  - time 패키지를 사용해서 DAYS를 구하고, 1000년 조건은 월, 일 value를 get해서 minus 연산으로 해결함.
    해당 연산의 경우, d1과 d2의 연차가 1000년을 넘는 경우와 1000년이되, 월, 일의 수가 동일한 경우에만 gg를 출력할 수 있도록 작성함. 
  #### 🔑 알게된 코드
  - time 패키지의 ChronoUnit의 사용법, LocalDate 코드 개념을 보강하였다.
  <pre>
  <code>
        // LocalDate
        LocalDate d1 = LocalDate.of(Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()));
        LocalDate d2 = LocalDate.of(Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()));

        // ChronoUnit
        long days = ChronoUnit.DAYS.between(d1, d2);
        long years = ChronoUnit.YEARS.between(d1, d2);

        // etc.. (time 패키지)
        long mdays = d1.getDayOfMonth() - d2.getDayOfMonth();
        long mmonths = d1.getMonthValue() - d2.getMonthValue();
        if (years > 1000 || (years == 1000 && mmonths == 0 && mdays == 0)) System.out.print("gg");
  </code>
  </pre>
  #### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    import java.util.*;
    import java.time.*;
    import java.time.temporal.ChronoUnit;
    class Main{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
        public void run() throws  IOException {
            st = new StringTokenizer(br.readLine());
            LocalDate d1 = LocalDate.of(Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()));
            st = new StringTokenizer(br.readLine());
            LocalDate d2 = LocalDate.of(Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()), Integer.parseInt(st.nextToken()));
            long days = ChronoUnit.DAYS.between(d1, d2);
            long mdays = d1.getDayOfMonth() - d2.getDayOfMonth();
            long mmonths = d1.getMonthValue() - d2.getMonthValue();
            long years = ChronoUnit.YEARS.between(d1, d2);
            if (years > 1000 || (years == 1000 && mmonths == 0 && mdays == 0)) System.out.print("gg");
            //else System.out.println(years + "\n" + mmonths + "\n" + mdays + "\n");
            else System.out.print("D-" + days);
            br.close();
        }
        public static void main(String[] args) {
            try{
                new Main().run();
            } catch (IOException e){
                System.err.println("오류 : " + e.getMessage());
            }
        }
    }
  </code>
  </pre>
    
