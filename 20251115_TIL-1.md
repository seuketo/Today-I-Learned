# Today-I-Learned

##### Date: 2025-11-15

# 1️⃣ 오늘 배운 것
- 상속 개념 이해
- Comparator, 람다식 연계 사용 이해.
- Queue, Deque 인터페이스 사용 이해.
- - -
# 2️⃣ 참고 문제 / 자료
1. 백준_11651번: 좌표 정렬하기 2
2. 백준_2164번: 카드2
- - -
# 3️⃣ 정리
  ## 1. 상속 개념 이해
  ### 🖥 사용 개념
  - extends
  - abstract
  - implements + interface(인터페이스)
- - -
  ### 🛠 개념 정리
  **1. extends**
  * 클래스에서 클래스로 필드와 메소드를 상속할 때 사용.
  * 형식은 다음과 같음. (부모 클래스는 기존처럼 사용하되, 자식 클래스는 extends 부모 클래스를 해줘야 상속이 가능함.)
    - 부모 클래스 : class (부모 클래스) { . . . }
    - 자식 클래스 : class (자식 클래스) extends (부모 클래스) { . . . }
  * 인터페이스는 상속하거나 상속 받을 수 없음.
  * 자식 클래스는 부모 클래스를 하나만 상속받을 수 있음.
  * 업캐스팅이 가능함. (자식 객체를 가리키는 변수의 타입을 부모 타입으로 선언할 수 있음.)
    - 자식 타입이 옳은 타입이지만, 상황에 따라 업캐스팅을 통해 부모 타입이 되는 것도 가능함.
  * 사용 가능한 경우는 다음과 같음.
    - 클래스에서 클래스로 상속하는 경우. (부모 클래스 단 하나만 소유 가능)
    - 인터페이스에서 인터페이스로 상속하는 경우 (여러 인터페이스 상속 가능 - extends (인터페이스 1), (인터페이스2), ... )
- - -
<pre>
<code>
  class Animal{
    void sound(){
      System.out.println("동물 소리");
    } 
  }

  class Dog extends Animal{
    @Override
    void sound(){
      System.out.println("멍멍");
    }

    void wagtail(){
      System.out.println("꼬리 살랑");
    }
  }

  ( . . . )
      
  (main 클래스에서)

  Dog d = new Dog();
      
  // 해당 코드가 업캐스팅의 경우. new 키워드로 Dog 객체를 가리키는 변수 생성을 했지만, 타입은 Animal임.
  Animal a = new Dog(); 
</code>
</pre>
- - -
  **2. abstract**
  * 클래스에 구현이 되지 않은 메소드가 존재할 경우 해당 메소드 앞과 class 앞에 abstract를 붙여야 함.
    <pre>
    <code>
      abstract class Dog{
        
        int foot;
        
        Dog(){ }
        
        abstract void walking();
        
        void sound() { 
          System.out.println("멍멍");
        }
      }
    </code>
    </pre>
  * 상속의 개념에 포함되지만, 이는 따지고 보면 수식언에 가까움.
    - extends, implements 처럼 누군가에게 상속을 받는 것이 아닌 자신의 상태를 나타내는 것.
    - 인터페이스는 애초에 abstract 상태와 다를 바가 없어서 굳이 사용 안 함. 일반적으로 클래스에서 사용됨.
    - 해당 표현이 사용되면, 사용된 메소드는 무조건 상속 받게 될 자식 클래스에서 구현이 되어야 함.
  
  * 일반적으로 공통 필드, 공통 메소드가 들어있는 클래스를 여러 개 구현해야하는 경우 사용함.
- - -
<pre>
<code>
  abstract class Animal{
    int hp;
    int energy;

    Animal(int hp, int energy){
      this.hp = hp;
      this.energy = energy;
    }
  
    void sound(){
      System.out.println("동물 소리");
    }

    abstract void walking();
  }

  class Dog extends Animal{
    @Override
    void sound(){
      System.out.println("멍멍");
    }
  
    @Override
    void walking(){
      System.out.println("자박자박");
    }

    void wagtail(){
      System.out.println("꼬리 살랑");
    }
  }
</code>
</pre>
- - -
  **3-1. implements**
  * implements는 인터페이스 -> 클래스로 상속이 가능함. 단, 역방향 상속은 불가능함.
  * implements는 extends 처럼 클래스 -> 클래스로 상속할 때 사용 X
  * extends와 달리 여러 개의 인터페이스를 부모로 해서 상속받을 수 있음.
  * extends가 부모 클래스의 정보를 받아서 사용하는 상속이라면, 해당 방법은 인터페이스를 필수 요소를 강제하지만
    그와 관련된 구현은 자식이 직접해야 함.
  * extends처럼 업캐스팅이 가능함. 단, 업캐스팅으로 만들어진 변수는 인터페이스에 적힌 메소드만 사용 가능함.
  * 사용 가능한 경우는 다음과 같음.
    - 인터페이스에서 클래스로 상속하는 경우 (여러 인터페이스를 상대로 상속 받기 가능 - implements (인터페이스1), (인터페이스2), ... )
- - -
  **3-2. interface(인터페이스)**
  * interface(인터페이스)는 상속을 해도 클래스에 영향을 주지 않음.
  * 인터페이스는 하나의 틀로써 해당 인터페이스에 포함된 요소를 모두 클래스에 구현해야 함.
  * 메소드 시그니처(메소드 규격)을 강제하지만, 구현은 작성되어있지 않기 때문에 자식 클래스가 구현해야만 함.
  * 상속을 위해선 implements를 무조건 사용해야 함.
- - -
<pre>
<code>
  interface Animal{
    void sound();
  }

  interface Walk{
    void walking();
  }

  class Dog implements Animal, Walk{
    int foot;
    int walk_count;

    Dog(int foot, int walk_count) {
      this.foot = foot;
      this.walk_count = walk_count;
    }
  
    @Override
    void sound() { 
      .  .  . 
    }
    @Override
    void walking() {
      .  .  .
    }
  
  } 

  ( . . . )
      
  (main 클래스에서)

  // 셋 다 가능. 단, 사용 가능한 메소드는 각 타입으로 지정된 인터페이스 OR 클래스 들어있는 메소드에 한정됨.
  Dog d = new Dog();
  Animal da = new Dog();
  Walk dw = new Dog();
</code>
</pre>
- - -
  ## 2. 백준_11651번: 좌표 정렬하기 2
  ### 🖥 사용 개념
  - 'Comparator'
  - '람다식'
- - -
  ### 🛠 개념 정리
  **1. 'Comparator'**
  * T 2개를 받아서, 누가 앞에 와야 하는지 숫자로 알려주는 함수.
  * 정렬이 될 때까지 반복함.
  * 람다식을 가장 많이 사용하는 곳 중 하나.
  * Comparator 인터페이스의 내용은 다음과 같다.
  <pre>
  <code>
    public interface Comparator<T>{
      int compare(T o1, T o2);
    } 
      // T는 자료형 (Integer 등...)
  </code>
  </pre>
  * compare() 메소드가 들어 있으며, compare(a, b)는 a - b의 결과값에 따라 정렬 결과가 달라짐.
    - result > 0 : a가 b보다 뒤 (a > b)
    - result == 0 : a와 b의 순서가 동일
    - result < 0 : a가 b보다 앞 (a < b)

- - -
**2. '람다식'**
  * 추상 메소드를 가진 함수형 인터페이스의 구현을 간단하게 쓰는 문법.
<pre>
<code>
  Comparator <Integer> comp = new Comparator<Integer>(){
        @Override
        public int compare (Integer a, Integer b){
          return a - b;
        }
      };
    
    // 해당 코드를 람다식으로 간단하게 만들면 다음과 같음.
    Comparator<Integer> comp = (a, b) -> a - b;
</code>
</pre>
  * 기본적인 형식은 다음과 같음.
    - **(매개변수, 매개변수) -> { 실행문들 }**
    - 매개변수는 대부분 자료형 생략 가능
    - 실행문 길이가 한 줄 정도면 {} / return도 생략 가능
- - -
<pre>
<code>
      Arrays.sort(arr, (a, b) -> {
        if (a[1] != b[1]) return a[1] - b[1];
        else return a[0] - b[0];
      });
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 해당 문제는 Comparator와 람다식을 연계해서 해결하였음.
  - Comparator를 사용할 때 조금 다른 점이 있었는데, 이는 Arrays.sort() 부분.
    - Arrays.sort() 함수 자체에 배열을 매개변수로 넣어야 하기 때문에 배열명을 넣었음.
    - 일반적으로 (a, b) -> {} 이런 식이지만, 해당 함수에선 (arr, (a, b) -> {}) 으로 사용했음.
  - b을 기준으로 먼저 비교해서 정렬 후, 동일한 b값을 가지는 경우 해당 위치의 a값을 비교해서 정렬함.
- - -
  ### 🤔 아쉬운 점
  * 조금 아쉬웠던 점은 결과값 출력까지 걸린 시간과 메모리 사용량이 생각보다 너무 많이 들었다는 점이다.
  * 해당 코드보다 더 효율적인 방법이 있는지 확인할 필요가 있을 것 같다.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    import java.util.*;
    
    public class a_11651{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
        StringBuilder sb = new StringBuilder();
        public void run() throws IOException{
            int n = Integer.parseInt(br.readLine());
            int[][] arr = new int[n][2];
            for (int i = 0; i < n; i++){
                st = new StringTokenizer(br.readLine());
                int a = Integer.parseInt(st.nextToken());
                int b = Integer.parseInt(st.nextToken());
                arr[i][0] = a;
                arr[i][1] = b;
            }
            Arrays.sort(arr, (a, b) -> {
                if (a[1] != b[1]) return a[1] - b[1];
                else return a[0] - b[0];
            });
            for (int i = 0; i < n; i++) sb.append(arr[i][0] + " " + arr[i][1]).append("\n");
            sb.trimToSize();
            System.out.println(sb);
            br.close();
    
        }
    
        public static void main(String[] args) throws IOException{
            try{
                new a_11651().run();
            } catch (IOException e){
                System.err.println(e.getMessage());
            }
        }
    }
  </code>
  </pre>
- - -
  ## 3. 백준_2164번_카 2
  ### 🖥 사용 개념
  - 'Queue, Deque 인터페이스'
- - -
  ### 🛠 개념 정리
  **1. 'Deque 인터페이스'**
  * Deque는 FIFO (선입선출) 방식.
  * Deque가 인터페이스 형식으로 java.function 패키지에 포함되어있는 것을 발견함.
  * 해당 인터페이스는 양쪽에서 입력과 출력 가능함.
  * Stack, Queue 둘 다 대체 가능한 인터페이스.
  * 사용 방법은 다음과 같음.
    <pre>
    <code>
      Deque<Integer> q = new ArrayDeque<>();

      // 순서대로 입력 (ex : 1, 2, 3, 4, 5)
        q.addLast();
      // 반대로 입력 (ex : 5, 4, 3, 2, 1)
        q.addFirst(); 
      // Deque 크기 확인
        q.size();

      // q.poll ~ 관련 함수들은 사용 시 해당 데이터를 일회성으로 선택 후 소멸됨. 
      // 해당 데이터를 저장하고 싶으면 변수 선언, 저장할 필요 없이 일회성으로 사용할 거라면 함수에 변수 대신 직접적으로 사용 추천
        
      // 맨 먼저 or 맨 위에 입력된 데이터 뽑기 (ex : (1, 2, 3) -> (2, 3))
        q.pollFirst();
      
      // 가장 마지막에 입력된 데이터 뽑기 (ex : (1, 2, 3) -> (1, 2))
        q.pollLast();
    </code>
    </pre>
- - -
<pre>
<code>
      Deque<Integer> q = new ArrayDeque<>();
      for (int i = 1; i <= n; i++) q.addLast(i);

      while (q.size() > 1){
          q.pollFirst();
          q.addLast(q.pollFirst());
      }
</code>
</pre>
- - -
  ### 🔍 해결 방법
  - 초기 해당 문제를 풀 때 동적 배열을 이용해서 해결해보려고 했었음.
  - 하지만 remove() 함수가 꽤 위험할 뿐더러 정확한 결과값을 여러 번의 시도에도 얻지 못 했었음.
  - 해당 문제의 경우 번갈아가면서 하나는 버리고 하나는 뒤로 보내야 하는 문제였기 때문에 Deque를 사용함.
  - Deque로 하나는 pollFirst()로 버리고, 하나는 addLast(pollFirst())로 뒤로 보냄으로써 해결함.
- - -
  ### 🤔 아쉬운 점
  * 해다 자료구조가 인터페이스로 존재한다는 사실을 몰랐었다.
    수업을 통해 배우지 못했던 부분이라 의문 조차도 가지지 못했던 것이 컸었던 거 같다.
- - -
  ### 🔔 전체 코드
  <pre>
  <code>
    import java.io.*;
    import java.util.*;
    
    public class a_2164 {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        public void run() throws IOException{
            int n = Integer.parseInt(br.readLine());
    
            Deque<Integer> q = new ArrayDeque<>();
            for (int i = 1; i <= n; i++) q.addLast(i);
    
            while (q.size() > 1){
                q.pollFirst();
                q.addLast(q.pollFirst());
            }
    
            System.out.println(q.peekFirst());
            br.close();
        }
    
        public static void main(String[] args) throws IOException{
            try{
                new a_2164().run();
            } catch (IOException e){
                System.err.println(e.getMessage());
            }
        }
    }
  </code>
  </pre>
- - -
